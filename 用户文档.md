# GhostFix User Manual | 用户文档

> **NFD→NFC Unicode Filename Repair Tool | NFD→NFC Unicode 文件名修复工具**  
> Version 版本 1.0.0 | Platform 适用平台: Windows 10/11 (PowerShell 5.1+)

---

## Table of Contents | 目录

1. [What Is This | 这是什么](#1-what-is-this--这是什么)
2. [Use Cases | 适用场景](#2-use-cases--适用场景)
3. [Installation & Running | 安装与运行](#3-installation--running--安装与运行)
4. [Usage Guide | 使用指南](#4-usage-guide--使用指南)
5. [Safety Notes | 安全说明](#5-safety-notes--安全说明)
6. [FAQ | 常见问题](#6-faq--常见问题)
7. [Technical Background | 技术背景](#7-technical-background--技术背景)

---

## 1. What Is This | 这是什么

GhostFix is a Windows GUI tool that **recursively scans a folder and repairs ghost files** — files whose names are corrupted by NFD (decomposed) Unicode encoding and therefore inaccessible on macOS.

GhostFix 是一个 Windows GUI 工具，用于**递归扫描并修复文件夹中的幽灵文件**——即那些因 NFD (分解形式) Unicode 编码导致文件名异常，在 macOS 上无法访问的文件。

Just three steps:
1. **Select** the target folder
2. **Scan** for ghost files
3. **Fix** with one click

只需三步：
1. **选择**目标文件夹
2. **扫描**幽灵文件
3. **一键修复**

---

## 2. Use Cases | 适用场景

### Typical Problem | 典型问题

When an NTFS-formatted external drive / SD card is read and written on macOS through the **Tuxera NTFS** driver, filenames containing the following characters are mistakenly written in NFD decomposed form:

当 NTFS 格式的外置硬盘 / SD 卡通过 **Tuxera NTFS** 驱动在 macOS 上读写时，含以下字符的文件名会被错误地以 NFD 分解形式写入：

| Character Type 字符类型 | Example 示例 | NFD Form (broken) NFD 形式（问题） | NFC Form (normal) NFC 形式（正常） |
|---------|------|-----------------|-----------------|
| French accents 法文变音 | é | e + ́ (2 codepoints 2码点) | é (1 codepoint 1码点) |
| German umlauts 德文变音 | ü | u + ̈ (2 codepoints 2码点) | ü (1 codepoint 1码点) |
| Japanese voiced sounds 日文浊音 | ド | ト + ゙ (2 codepoints 2码点) | ド (1 codepoint 1码点) |
| Semi-voiced sounds 半浊音 | プ | フ + ゚ (2 codepoints 2码点) | プ (1 codepoint 1码点) |

**Symptom | 症状**: The file is visible in macOS Finder but cannot be opened; the terminal `ls` lists it but `stat` reports "No such file or directory".

文件在 macOS Finder 中可见但无法打开，终端 `ls` 能列出但 `stat` 报错 "No such file or directory"。

### Typical Workflow | 典型工作流

```
1. Insert the SD card into a Windows PC            | SD卡插入 Windows 电脑
2. Open GhostFix and pick the music folder on it   | 打开 GhostFix，选择 SD 卡上的音乐文件夹
3. Click "Scan Ghosts" → review the results        | 点击"扫描幽灵文件" → 查看检测结果
4. Click "Fix All" → filename encoding repaired    | 点击"修复全部幽灵" → 文件名编码修正
5. Export the log for your records                 | 导出日志留档
6. Put the card back into the Mac → files work ✅  | SD卡插回 Mac → 文件正常访问 ✅
```

---

## 3. Installation & Running | 安装与运行

### System Requirements | 系统要求

- Windows 10 or Windows 11 | Windows 10 或 Windows 11
- PowerShell 5.1 or later (built-in, no installation needed) | PowerShell 5.1 或更高版本（系统自带，无需安装）

### How to Run | 运行方法

#### Method 1: Double-click (recommended) | 方法一：双击运行（推荐）

1. Locate the `GhostFix.ps1` file | 找到 `GhostFix.ps1` 文件
2. **Right-click** → **"Run with PowerShell"** | **右键** → **"使用 PowerShell 运行"**

If double-clicking opens an editor instead, use Method 2.

如果双击直接打开了编辑器，请改用方法二。

#### Method 2: Command line | 方法二：命令行运行

```powershell
powershell -ExecutionPolicy Bypass -File "C:\...\GhostFix\GhostFix.ps1"
```

#### Method 3: Inside PowerShell | 方法三：PowerShell 中运行

```powershell
cd "C:\...\GhostFix"
.\GhostFix.ps1
```

> **Note | 注意**: If you get a "cannot load script" error, run this first:
> 如果遇到"无法加载脚本"的错误，请先执行：
> ```powershell
> Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
> ```

### File Structure | 文件结构

```
GhostFix/
├── GhostFix.ps1                 ← Main application 主程序
├── 启动GhostFix.bat              ← One-click launcher 一键启动脚本
├── 用户文档.md                    ← This document (bilingual) 本文档（双语）
├── README.md                    ← Bilingual project readme 双语项目说明
├── LICENSE                      ← MIT license (EN + ZH) MIT 许可证（英 + 中）
├── .gitignore
├── screenshots/                 ← UI screenshots 界面截图
├── scripts/
│   └── generate_screenshots.ps1 ← Screenshot generator 截图生成脚本
└── test/                        ← Test data (removable) 测试数据（可删除）
```

**No third-party software needed.** GhostFix is built entirely with Windows' built-in PowerShell + WinForms.

**不需要安装任何第三方软件**。GhostFix 完全使用 Windows 自带的 PowerShell + WinForms 构建。

---

## 4. Usage Guide | 使用指南

### Step 1: Select a Folder | 选择文件夹

Click **📂 Browse 浏览** to choose the folder to scan, or paste a path directly into the input box.

点击 **📂 Browse 浏览** 按钮，选择要扫描的文件夹，或直接粘贴路径到输入框。

### Step 2: Scan | 扫描

Click **🔍 Scan Ghosts 扫描幽灵文件**. GhostFix recursively walks every file in the folder and detects NFD-encoded filenames.

点击 **🔍 Scan Ghosts 扫描幽灵文件**，GhostFix 会递归遍历文件夹中所有文件，检测 NFD 编码的文件名。

Meaning of each column in the result table:

扫描结果表格各列含义：

| Column 列名 | Description 说明 |
|-----|------|
| Status 状态 | ⚠️ Pending 待修复 / ✅ Fixed 已修复 / ❌ Failed 失败 |
| Directory 子目录 | Path of the file relative to the target folder 文件相对于目标文件夹的路径 |
| Original Name (NFD) 原文件名 | Current filename (decomposed form) 当前文件名（分解形式） |
| Fixed Name (NFC) 修复后 | Filename after repair (composed form) 修复后的文件名（预组合形式） |
| Size 大小 | File size 文件大小 |
| Error 错误信息 | Error message when a fix fails 修复失败时的错误说明 |

Statistics shown at the bottom: **Total files / Ghosts / Fixed / Failed**.

底部会显示统计：**Total 总文件数 / Ghosts 幽灵数 / Fixed 已修复 / Failed 失败**。

### Step 3: Fix | 修复

After confirming the scan results, click **🔧 Fix All 修复全部幽灵**.

确认扫描结果无误后，点击 **🔧 Fix All 修复全部幽灵**。

The program will:
1. Show a confirmation dialog with the number of files to fix
2. Rename each file from its NFD name to its NFC name, one by one
3. Update the progress bar and the status column in real time
4. Show a result summary when everything is done

程序会：
1. 弹出确认对话框，显示待修复文件数
2. 逐个将文件从 NFD 名重命名为 NFC 名
3. 实时更新进度条和状态列
4. 全部完成后显示结果统计

### Step 4: Export Log | 导出日志

Click **📋 Export Log 导出日志** to save the fix records as:

点击 **📋 Export Log 导出日志**，将修复记录保存为：

- **CSV** format (recommended, opens in Excel) | **CSV** 格式（推荐，可用 Excel 打开）
- **JSON** format (for programmatic processing) | **JSON** 格式（供程序化处理）
- **TXT** format (plain-text reading) | **TXT** 格式（纯文本阅读）

### Keyboard Shortcuts | 快捷键

| Shortcut 快捷键 | Function 功能 |
|-------|------|
| `Ctrl + F` | Start scan 开始扫描 |
| `Ctrl + R` | Start fix 开始修复 |
| `Ctrl + S` | Export log 导出日志 |

---

## 5. Safety Notes | 安全说明

### Renames only, never touches content | 只改文件名，不改内容

GhostFix's operation is a **pure rename** (`Rename-Item`); not a single byte of file content is modified. The MD5/SHA256 hashes of a file are identical before and after the operation.

GhostFix 的操作是**纯重命名**（`Rename-Item`），文件内容一个字节都不会被修改。操作前后文件的 MD5/SHA256 哈希值完全一致。

### Atomic on NTFS | NTFS 原子操作

On the NTFS filesystem a rename is atomic — it either fully succeeds or has no effect at all; there is no "half-renamed" state.

在 NTFS 文件系统上，重命名是原子操作——要么完全成功，要么完全不生效，不会出现"改了一半"的状态。

### Normal files are never touched | 不会误伤正常文件

GhostFix detects NFD encoding with an **Ordinal string comparison**. Pure ASCII filenames (e.g. `hello.mp3`) and already-NFC filenames (e.g. `测试.mp3`) are never misjudged as ghost files.

GhostFix 使用 **Ordinal 字符串比较**检测 NFD 编码，纯 ASCII 文件名（如 `hello.mp3`）和已经是 NFC 形式的文件名（如 `测试.mp3`）不会被误判为幽灵文件。

### Recommendations | 建议

- First-time users should try it on a test folder first | 首次使用时，建议先在测试文件夹上试用
- Back up important data before operating | 对重要数据，建议操作前做好备份
- Use "Scan" first to see the scope of impact, then click "Fix" | 可以先用"扫描"功能查看影响范围，确认后再点击"修复"

---

## 6. FAQ | 常见问题

### Q: Why does the scan report 0 ghost files? | 为什么扫描结果显示 0 个幽灵文件？

A: That's good news! It means every filename in the folder is already in NFC normalized form — nothing needs fixing.

答: 这是好消息！说明文件夹中所有文件名都已是 NFC 规范形式，无需修复。

### Q: Files are still inaccessible on macOS after the fix? | 修复后，文件在 macOS 上仍然无法访问？

A: Please check:
1. Is the SD card / drive properly inserted in the Mac?
2. Did the GhostFix fix really succeed? (Re-scan after fixing; it should show 0 ghosts.)
3. Run `ls -la` in the macOS terminal to check file permissions.

答: 请检查：
1. SD 卡/硬盘是否正确插入 Mac
2. 确认 GhostFix 修复确实成功（修复后重新扫描一次，应为 0 个幽灵）
3. 在 macOS 终端运行 `ls -la` 检查文件权限

### Q: Some files fail to fix with "access denied"? | 某些文件修复失败，显示"拒绝访问"？

A: Possible causes:
- The file is in use by another program (close that program and retry)
- The file is read-only (right-click → Properties → uncheck Read-only)
- The current user has no write permission on the folder

答: 可能原因：
- 文件正在被其他程序使用（关闭相关程序后重试）
- 文件为只读属性（右键 → 属性 → 取消只读）
- 当前用户没有该文件夹的写入权限

### Q: Ghosts and normal files are mixed — can I fix only the ghosts? | 幽灵文件和正常文件混在一起，能否只修复幽灵文件？

A: That is exactly how GhostFix is designed — it only modifies filenames detected as NFD-encoded; normal files are completely unaffected.

答: GhostFix 的设计就是如此——它只会修改被检测为 NFD 编码的文件名，正常文件完全不受影响。

### Q: Does it run on macOS? | 能在 macOS 上运行吗？

A: GhostFix currently supports Windows only. The repair relies on the Windows native NTFS driver's path handling. On macOS, the Tuxera bug makes the paths themselves unreachable, so the rename cannot be performed.

答: GhostFix 目前仅支持 Windows。这是因为修复操作依赖 Windows 原生 NTFS 驱动的路径处理能力。在 macOS 上，Tuxera bug 导致路径本身就不可达，无法执行重命名。

### Q: Which file types are supported? | 支持哪些文件类型？

A: **All file types** — music, videos, documents, images, and so on. GhostFix only checks the filename encoding; it does not care about file content.

答: 支持**所有文件类型**——音乐、视频、文档、图片等等。GhostFix 只检查文件名编码，不关心文件内容。

---

## 7. Technical Background | 技术背景

### Unicode Normalization Forms | Unicode 规范化形式

The Unicode standard defines two commonly used normalization forms:

Unicode 标准定义了两种常用的规范化形式：

| Form 形式 | Full Name 全称 | Description 说明 | Example 示例 |
|-----|------|-----|------|
| **NFC** | Normalization Form C (Composed) 预组合 | Composed characters, single codepoints wherever possible 预组合字符，尽可能使用单码点 | `é` = U+00E9 |
| **NFD** | Normalization Form D (Decomposed) 分解 | Decomposed characters, base character + combining marks 分解字符，基本字符 + 组合符号 | `é` = U+0065 + U+0301 |

- **Windows** (NTFS) and **modern macOS** (APFS) both prefer NFC | **Windows** (NTFS) 和 **现代 macOS** (APFS) 都偏好 NFC
- **Classic macOS** (HFS+) forces NFD | **经典 macOS** (HFS+) 强制使用 NFD
- The **Tuxera NTFS** driver produces NFD→NTFS writes, creating ghost files | **Tuxera NTFS** 驱动在处理过程中产生 NFD→NTFS 写入，导致幽灵文件

### GhostFix Detection Algorithm | GhostFix 检测算法

```powershell
# Core detection logic | 核心检测逻辑
$nfc = $filename.Normalize(FormC)
$isGhost = ($filename -ne $nfc)  # Ordinal comparison | Ordinal 比较
```

If a filename differs from its NFC-normalized form under Ordinal comparison, the filename is currently in NFD form and needs repair.

如果文件名与其 NFC 规范化形式在 Ordinal 比较下不同，说明文件名当前为 NFD 形式，需要修复。

### Fix Method | 修复方法

```powershell
Rename-Item -LiteralPath <NFD path NFD路径> -NewName <NFC filename NFC文件名>
```

`Rename-Item` on NTFS is an atomic operation — safe and reliable.

NTFS 上的 `Rename-Item` 是原子操作，安全可靠。

---

*GhostFix v1.0 — No place left for ghost files to hide. | 让幽灵文件无处藏身。*
