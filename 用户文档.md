# GhostFix 用户文档

> **NFD→NFC Unicode 文件名修复工具**  
> 版本 1.0.0 | 适用平台: Windows 10/11 (PowerShell 5.1+)

---

## 目录

1. [这是什么](#1-这是什么)
2. [适用场景](#2-适用场景)
3. [安装与运行](#3-安装与运行)
4. [使用指南](#4-使用指南)
5. [安全说明](#5-安全说明)
6. [常见问题](#6-常见问题)
7. [技术背景](#7-技术背景)

---

## 1. 这是什么

GhostFix 是一个 Windows GUI 工具，用于**递归扫描并修复文件夹中的幽灵文件**——即那些因 NFD (分解形式) Unicode 编码导致文件名异常，在 macOS 上无法访问的文件。

只需三步：
1. **选择**目标文件夹
2. **扫描**幽灵文件
3. **一键修复**

---

## 2. 适用场景

### 典型问题

当 NTFS 格式的外置硬盘 / SD 卡通过 **Tuxera NTFS** 驱动在 macOS 上读写时，含以下字符的文件名会被错误地以 NFD 分解形式写入：

| 字符类型 | 示例 | NFD 形式（问题） | NFC 形式（正常） |
|---------|------|-----------------|-----------------|
| 法文变音 | é | e + ́ (2码点) | é (1码点) |
| 德文变音 | ü | u + ̈ (2码点) | ü (1码点) |
| 日文浊音 | ド | ト + ゙ (2码点) | ド (1码点) |
| 半浊音 | プ | フ + ゚ (2码点) | プ (1码点) |

**症状**：文件在 macOS Finder 中可见但无法打开，终端 `ls` 列出但 `stat` 报错 "No such file or directory"。

### 典型工作流

```
1. SD卡插入 Windows 电脑
2. 打开 GhostFix，选择 SD 卡上的音乐文件夹
3. 点击"扫描幽灵文件" → 查看检测结果
4. 点击"修复全部幽灵" → 文件名编码修正
5. 导出日志留档
6. SD卡插回 Mac → 文件正常访问 ✅
```

---

## 3. 安装与运行

### 系统要求

- Windows 10 或 Windows 11
- PowerShell 5.1 或更高版本（系统自带，无需安装）

### 运行方法

#### 方法一：双击运行（推荐）

1. 找到 `GhostFix.ps1` 文件
2. **右键** → **"使用 PowerShell 运行"**

如果双击直接打开了编辑器，请改用方法二。

#### 方法二：命令行运行

```powershell
powershell -ExecutionPolicy Bypass -File "C:\...\GhostFix\GhostFix.ps1"
```

#### 方法三：PowerShell 中运行

```powershell
cd "C:\...\GhostFix"
.\GhostFix.ps1
```

> **注意**：如果遇到"无法加载脚本"的错误，请先执行：
> ```powershell
> Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
> ```

### 文件结构

```
GhostFix/
├── GhostFix.ps1      ← 主程序
├── 用户文档.md        ← 本文档
└── test/             ← 测试数据（可删除）
```

**不需要安装任何第三方软件**。GhostFix 完全使用 Windows 自带的 PowerShell + WinForms 构建。

---

## 4. 使用指南

### Step 1: 选择文件夹

点击 **📂 浏览** 按钮，选择要扫描的文件夹，或直接粘贴路径到输入框。

### Step 2: 扫描

点击 **🔍 扫描幽灵文件**，GhostFix 会递归遍历文件夹中所有文件，检测 NFD 编码的文件名。

扫描结果表格各列含义：

| 列名 | 说明 |
|-----|------|
| 状态 | ⚠️ 待修复 / ✅ 已修复 / ❌ 失败 |
| 子目录 | 文件相对于目标文件夹的路径 |
| 原文件名 (NFD) | 当前文件名（分解形式） |
| 修复后 (NFC) | 修复后的文件名（预组合形式） |
| 大小 | 文件大小 |
| 错误信息 | 修复失败时的错误说明 |

底部会显示统计：**总文件数 / 幽灵数 / 已修复 / 失败**。

### Step 3: 修复

确认扫描结果无误后，点击 **🔧 修复全部幽灵**。

程序会：
1. 弹出确认对话框，显示待修复文件数
2. 逐个将文件从 NFD 名重命名为 NFC 名
3. 实时更新进度条和状态列
4. 全部完成后显示结果统计

### Step 4: 导出日志

点击 **📋 导出日志**，将修复记录保存为：
- **CSV** 格式（推荐，可用 Excel 打开）
- **JSON** 格式（供程序化处理）
- **TXT** 格式（纯文本阅读）

### 快捷键

| 快捷键 | 功能 |
|-------|------|
| `Ctrl + F` | 开始扫描 |
| `Ctrl + R` | 开始修复 |
| `Ctrl + S` | 导出日志 |

---

## 5. 安全说明

### 只改文件名，不改内容

GhostFix 的操作是**纯重命名**（`Rename-Item`），文件内容一个字节都不会被修改。操作前后文件的 MD5/SHA256 哈希值完全一致。

### NTFS 原子操作

在 NTFS 文件系统上，重命名是原子操作——要么完全成功，要么完全不生效，不会出现"改了一半"的状态。

### 不会误伤正常文件

GhostFix 使用 **Ordinal 字符串比较**检测 NFD 编码，纯 ASCII 文件名（如 `hello.mp3`）和已经是 NFC 形式的文件名（如 `测试.mp3`）不会被误判为幽灵文件。

### 建议

- 首次使用时，建议先在测试文件夹上试用
- 对重要数据，建议操作前做好备份
- 可以先用"扫描"功能查看影响范围，确认后再点击"修复"

---

## 6. 常见问题

### Q: 为什么扫描结果显示 0 个幽灵文件？

A: 这是好消息！说明文件夹中所有文件名都已是 NFC 规范形式，无需修复。

### Q: 修复后，文件在 macOS 上仍然无法访问？

A: 请检查：
1. SD 卡/硬盘是否正确插入 Mac
2. 确认 GhostFix 修复确实成功（修复后重新扫描一次，应为 0 个幽灵）
3. 在 macOS 终端运行 `ls -la` 检查文件权限

### Q: 某些文件修复失败，显示"拒绝访问"？

A: 可能原因：
- 文件正在被其他程序使用（关闭相关程序后重试）
- 文件为只读属性（右键 → 属性 → 取消只读）
- 当前用户没有该文件夹的写入权限

### Q: 幽灵文件和正常文件混在一起，能否只修复幽灵文件？

A: GhostFix 的设计就是如此——它只会修改被检测为 NFD 编码的文件名，正常文件完全不受影响。

### Q: 能在 macOS 上运行吗？

A: GhostFix 目前仅支持 Windows。这是因为修复操作依赖 Windows 原生 NTFS 驱动的路径处理能力。在 macOS 上，Tuxera bug 导致路径本身就不可达，无法执行重命名。

### Q: 支持哪些文件类型？

A: 支持**所有文件类型**——音乐、视频、文档、图片等等。GhostFix 只检查文件名编码，不关心文件内容。

---

## 7. 技术背景

### Unicode 规范化形式

Unicode 标准定义了两种常用的规范化形式：

| 形式 | 全称 | 说明 | 示例 |
|-----|------|-----|------|
| **NFC** | Normalization Form C (Composed) | 预组合字符，尽可能使用单码点 | `é` = U+00E9 |
| **NFD** | Normalization Form D (Decomposed) | 分解字符，基本字符 + 组合符号 | `é` = U+0065 + U+0301 |

- **Windows** (NTFS) 和 **现代 macOS** (APFS) 都偏好 NFC
- **经典 macOS** (HFS+) 强制使用 NFD
- **Tuxera NTFS** 驱动在处理过程中产生 NFD→NTFS 写入，导致幽灵文件

### GhostFix 检测算法

```powershell
# 核心检测逻辑
$nfc = $filename.Normalize(FormC)
$isGhost = ($filename -ne $nfc)  # Ordinal 比较
```

如果文件名与其 NFC 规范化形式在 Ordinal 比较下不同，说明文件名当前为 NFD 形式，需要修复。

### 修复方法

```powershell
Rename-Item -LiteralPath <NFD路径> -NewName <NFC文件名>
```

NTFS 上的 `Rename-Item` 是原子操作，安全可靠。

---

*GhostFix v1.0 — 让幽灵文件无处藏身。*
